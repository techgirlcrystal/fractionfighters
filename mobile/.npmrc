# Clerk's Expo SDK pulls in @clerk/clerk-js, which declares react-dom as a peer.
# React Native doesn't use react-dom, so that phantom peer (and its strict React
# version range) would otherwise block every install against Expo's pinned React.
# legacy-peer-deps lets npm install without enforcing those unused web peers.
legacy-peer-deps=true
